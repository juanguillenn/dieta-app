import test from'node:test';import assert from'node:assert/strict';import{makeShopping,selectedText,selectedPayload,displayAmount}from'../shopping.mjs';
const days=[{name:'Lunes',date:'2026-09-14',meals:{Comida:[{name:'Pechuga de pollo',qty:150,unit:'g'},{name:'Tomates',qty:1,unit:'ud'}]}},{name:'Martes',date:'2026-09-15',meals:{Cena:[{name:'Pechuga de pollo',qty:.2,unit:'kg'},{name:'Aceite de oliva',qty:0,unit:''}]}}];
test('agrupa ingredientes, convierte unidades y conserva apariciones',()=>{let items=makeShopping(days,2,(_d,m)=>`Receta ${m}`),pollo=items.find(x=>x.name==='Pechuga de pollo');assert.equal(pollo.quantity,700);assert.equal(pollo.unit,'g');assert.equal(pollo.origins.length,2);assert.equal(displayAmount(pollo),'700 g')});
test('genera texto estable sólo con los elementos marcados',()=>{let items=makeShopping(days,2);items.find(x=>x.name==='Tomates').selected=false;assert.match(selectedText(items),/^Pechuga de pollo \(700 g\)$/m);assert.doesNotMatch(selectedText(items),/Tomates/);assert.match(selectedText(items),/^Aceite de oliva$/m)});
test('payload mantiene nombre, cantidad y categoría estructurados',()=>{let payload=selectedPayload(makeShopping(days,1)),pollo=payload.find(x=>x.name==='Pechuga de pollo');assert.deepEqual(pollo,{name:'Pechuga de pollo',quantity:'350 g',category:'Carne y pescado'})});
